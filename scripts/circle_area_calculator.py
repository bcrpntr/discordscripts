# This script imports the math module, defines a function to calculate the area of a circle based on a given radius, and prints the result for a radius of 5.

import math

def calculate_circle_area(radius):
    return math.pi * radius ** 2

print(calculate_circle_area(5))