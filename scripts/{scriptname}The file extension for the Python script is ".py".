# This script calculates the area of a circle given the radius
# Import the math module to access the constant pi
import math

# Prompt user for the radius of the circle
radius = float(input("Enter the radius of the circle: "))

# Calculate the area using the formula A = pi * r^2
area = math.pi * (radius ** 2)

# Format the output to display only two decimal places
rounded_area = round(area, 2)

# Print the calculated area
print(f"The area of the circle is: {rounded_area}")