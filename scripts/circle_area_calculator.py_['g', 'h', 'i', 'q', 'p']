# This script imports the math module and defines a function to calculate the area of a circle using the given radius.

import math

# Define a function to calculate the area of a circle.
# The function takes a single parameter, the radius of the circle.
# It uses the pi constant from the math module and the radius to calculate the area.
# It returns the calculated area.
def calculate_circle_area(radius):
    return math.pi * radius ** 2

# Call the 'calculate_circle_area' function with a radius of 5 and print the result to the console.
print(calculate_circle_area(5))