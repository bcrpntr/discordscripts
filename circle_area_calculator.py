"""
This script calculates the area of a circle given the radius.

First, the math module is imported to give access to the pi constant.

Next, the calculate_circle_area function is defined. This function takes one parameter, radius, and calculates the area of a circle with that radius. The area is calculated by multiplying pi by the radius squared.

Finally, the script prompts the user for the radius of the circle. The input is converted to a float